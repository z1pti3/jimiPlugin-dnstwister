import textract

from plugins.datasee.includes import processor

class _tract(processor.processor):
    supportedFileTypes = ["eml","msg","gif","jpg","jpeg","png","html","htm"] #add doc

    def process(self):
        text = str(textract.process(self.dsFile.path, language="eng"))     
        text = text.replace("\\n"," ").replace("\\r"," ")
        print(self.dsFile.path)
        print(text)
        
        return [processor.processItem(1,text)]

