rule keywordEmail : keyword email
{
  strings:
    $string1 = "Email"  
    $string2 = "email"
    $string3 = "EMAIL" 
  condition:
    any of them
}

rule regEmail : reg email
{
  strings:
    $email  = /([\w\.=-]+@[\w\.-]+\.[\w]{2,3})/ fullword
  condition:
    any of them
}
